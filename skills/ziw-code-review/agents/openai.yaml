interface:
  display_name: "Code Review"
  short_description: "Review code changes"
  default_prompt: "Use $ziw-code-review to review this branch before opening a PR."
policy:
  allow_implicit_invocation: true
