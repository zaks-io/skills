interface:
  display_name: "Agent Orchestrator"
  short_description: "Orchestrate tracked work"
  default_prompt: "Use $workflow-agent-orchestrator to orchestrate a specific ticket set, tracker filter, project, or until-clear backlog loop by selecting startable issues, launching implementation workers, requesting Agent Review, and managing PR state."
policy:
  allow_implicit_invocation: false
